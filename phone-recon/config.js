// Fill these in before running the app.
// Keys are only stored on your device — never sent anywhere except the respective API.

export const CONFIG = {
  // twilio.com/try-twilio → Console → Account Info
  TWILIO_ACCOUNT_SID: '',
  TWILIO_AUTH_TOKEN: '',

  // numlookupapi.com → Dashboard
  NUMLOOKUP_API_KEY: '',

  // haveibeenpwned.com/API/Key ($3.50/mo)
  HIBP_API_KEY: '',
};
